/**
 * Events barrel.
 *
 * Centralised event registry + dispatcher. See `registry.ts` for the
 * catalog and `dispatcher.ts` for the runtime.
 *
 * @module events
 */

export * from './registry.js';
export * from './dispatcher.js';
