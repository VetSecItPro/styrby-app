# Compiled Rust artifacts
target/
*.node

# npm / node_modules
node_modules/
dist/

# napi-rs generated bindings
# WHY: These are generated by `napi build` and differ per platform.
# They must NOT be committed — each CI job generates its platform's binary.
styrby-native.*.node

# Build logs
*.log

# macOS
.DS_Store
