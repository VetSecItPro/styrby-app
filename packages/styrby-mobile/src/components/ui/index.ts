/**
 * UI Primitives — Barrel exports.
 *
 * Shared low-level UI components that are used across multiple screens
 * and should never be redefined inline. Per CLAUDE.md component-first
 * architecture rules.
 */

export { SettingRow } from './SettingRow';
export type { SettingRowProps } from './SettingRow';

export { SectionHeader } from './SectionHeader';
export type { SectionHeaderProps } from './SectionHeader';
