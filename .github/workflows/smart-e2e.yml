name: Smart E2E Tests

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  e2e:
    # Skip on feature branches until merged to main
    if: github.ref == 'refs/heads/main' || github.base_ref == 'main' || github.base_ref == 'develop'
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "E2E Intelligence System installed"
          echo "Workflows will activate after merge to main"
          echo "Package: @vetsecitpro/e2e-intelligence"
