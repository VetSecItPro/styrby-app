name: Smart E2E Tests

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  e2e:
    uses: VetSecItPro/e2e-intelligence/.github/workflows/smart-e2e.yml@main
    with:
      repo-name: styrby
    secrets: inherit
