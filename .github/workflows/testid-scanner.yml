name: TestID Scanner

on:
  schedule:
    - cron: '0 0 * * 1' # Weekly on Monday midnight UTC
  workflow_dispatch:     # Manual trigger

permissions:
  contents: write
  pull-requests: write

jobs:
  scan:
    # Only run on main branch
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "TestID Scanner installed"
          echo "Will run weekly after merge to main"
          echo "Package: @vetsecitpro/e2e-intelligence"
